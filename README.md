# OpenAudible to AudioBookShelf

This script automates the process of moving audiobook files from OpenAudible or Libation to an organized folder structure and updates AudioBookShelf accordingly. It handles file organization, metadata mapping, and interaction with the AudioBookShelf API.

## Features

* **Automated File Organization:** Organizes audiobooks into a structured directory based on author and series.
* **Multiple Download Sources:** Supports both OpenAudible and Libation as download sources.
* **Metadata Mapping:** Extracts relevant metadata from JSON files generated by either OpenAudible or Libation.
* **AudioBookShelf Integration:** Scans the library, matches books with Audible, and updates AudioBookShelf.
* **Profanity Cleaning:** Optional integration with MonkeyPlug for automated profanity removal using remote Whisper transcription.
* **File Management:** Optionally cleans up source folders after processing (for Libation).
* **Error Handling and Logging:** Provides informative logging and error messages.
* **Desktop Notifications:** Uses notify-send to display processing status.
* **Configurable:** Allows customization through command-line arguments.
* **CLI or YAML Arguments:** Accepts either CLI flags or a yaml file with arguments

## Prerequisites

* **Python 3.8+:** Ensure Python 3.8 or higher is installed on your system.
* **Required Python Packages:** Install the necessary packages using:

```bash
pip install -r requirements.txt
```

  Core dependencies include:
  - `requests` - For API communication
  - `pyyaml` - For YAML configuration file support
  - `openai` - For AI-powered search (optional feature)
  - `pydantic` - For structured data validation
  - `bs4` (BeautifulSoup) - For web scraping support
  
  Development dependencies (optional, for contributors):
  - `black`, `flake8`, `mypy`, `isort` - Code formatting and linting
  - `pytest`, `pytest-cov`, `pytest-mock` - Testing framework

* **OpenAudible or Libation:** This script assumes you have either OpenAudible or Libation installed and configured.
* **AudioBookShelf:** You need a running instance of AudioBookShelf and its API details.
* **notify-send:** For desktop notifications.

> [!NOTE]
> **Libation Auto-Export:** As of the latest version, if you're using Libation and haven't provided a `--books-json-path` or the file doesn't exist, the script will automatically generate `libation.json` for you by calling `libationcli export`. The file will be created in your `--source-audio-book-directory` (where your Libation audiobooks are stored). This means you no longer need to manually export your library! However, you must have `libationcli` available in your PATH.

## Usage

The script can be run from the command line with various arguments or using a YAML configuration file. Here are examples for both methods:

### Command Line Usage

Libation example (with auto-generation - recommended, requires libationcli in PATH):

```bash
python openaudible_to_ab.py \
  --server-url "http://audiobooks.example.com" \
  --abs-api-token "YOUR_API_TOKEN" \
  --library-id "1cc175ca-88b9-4910-abe5-bf10b8aaa702" \
  --source-audio-book-directory "~/Libation/Books/" \
  --destination-book-directory "/AudioBookShelf/library1" \
  --log-file-path "/tmp/book_processing.txt" \
  --audio-file-extension ".m4b" \
  --purchased-how-long-ago 7 \
  --download-program "Libation" \
  --libation-folder-cleanup True
```

> [!TIP]
> Notice that `--books-json-path` is not required! The script will automatically generate `libation.json` in your source audio book directory (`~/Libation/Books/` in this example).

Libation example (with FileLocationsV2.json and explicit books.json path):

```bash
python openaudible_to_ab.py \
  --server-url "http://audiobooks.example.com" \
  --abs-api-token "YOUR_API_TOKEN" \
  --library-id "1cc175ca-88b9-4910-abe5-bf10b8aaa702" \
  --books-json-path "~/Libation/books.json" \
  --libation-file-locations-path "~/Libation/FileLocationsV2.json" \
  --source-audio-book-directory "~/Libation/Books/" \
  --destination-book-directory "/AudioBookShelf/library1" \
  --log-file-path "/tmp/book_processing.txt" \
  --audio-file-extension ".m4b" \
  --purchased-how-long-ago 7 \
  --download-program "Libation" \
  --libation-folder-cleanup True
```

Libation example (without FileLocationsV2.json - legacy mode):

```bash
python openaudible_to_ab.py \
  --server-url "http://audiobooks.example.com" \
  --abs-api-token "YOUR_API_TOKEN" \
  --library-id "1cc175ca-88b9-4910-abe5-bf10b8aaa702" \
  --books-json-path "~/Libation/books.json" \
  --source-audio-book-directory "~/Libation/Books/" \
  --destination-book-directory "/AudioBookShelf/library1" \
  --log-file-path "/tmp/book_processing.txt" \
  --audio-file-extension ".m4b" \
  --purchased-how-long-ago 7 \
  --download-program "Libation" \
  --libation-folder-cleanup True
```

OpenAudible Example:
```bash
python openaudible_to_ab.py \
  --server-url "http://audiobooks.example.com" \
  --abs-api-token "YOUR_API_TOKEN" \
  --library-id "1cc175ca-88b9-4910-abe5-bf10b8aaa702" \
  --books-json-path "~/OpenAudible/books.json" \
  --source-audio-book-directory "~/OpenAudible/books/" \
  --destination-book-directory "/AudioBookShelf/library1" \
  --log-file-path "/tmp/book_processing.txt" \
  --audio-file-extension ".m4b" \
  --purchased-how-long-ago 7 \
  --download-program "OpenAudible"
```

> [!NOTE]
> There is an example YAML file `arguments.yaml` which is included in the repo.
> This file currently looks like:
> ```yaml
>  # AudioBookShelf server configuration
>  server_url: "http://example.com"
>  abs_api_token: ""
>  library_id: ""
>  
>  # Book source configuration
>  download_program: OpenAudible  # Options: OpenAudible or Libation
>  books_json_path: ""  # Optional for Libation - will be auto-generated if not provided
>  
>  # Directory paths
>  source_audio_book_directory: "/tmp/OpenAudible/books"
>  destination_book_directory: ""
>  
>  # File handling options
>  audio_file_extension: ".m4b"
>  copy_instead_of_move: false  # Preserves chunks when profanity cleaning is enabled
>  
>  # Libation-specific options
>  libation_folder_cleanup: false
>  libation_file_locations_path: ""
>  
>  # Processing options
>  purchased_how_long_ago: 7  # Number of days to look back (0 = all books)
>  
>  # Logging
>  log_file_path: "/tmp/book_processing.txt"  # Timestamp will be added automatically (e.g., book_processing_20260104_140530.txt)
>  debug: false  # Includes censorship report for profanity cleaning
>  
>  # Profanity Cleaning Configuration (Optional)
>  enable_profanity_cleaning: false
>  remote_whisper_url: ""  # Required if profanity cleaning enabled
>  swears_file: ""  # Optional - path to custom swears file
>  working_directory: "/tmp/monkeyplug-cleaning"
>  save_transcripts: false
>  timeout: 600  # Timeout in seconds (10 minutes)
>  poll_interval: 30  # Check transcription status every 30 seconds
>  beep_mode: false
>  confidence_threshold: 0.75  # 0.0-1.0, lower = more aggressive
>```

## Arguments

* **--abs-api-token:** Your AudioBookShelf API token.
* **--books-json-path:** Path to the JSON file exported from OpenAudible or Libation containing book information. (Optional for Libation - will be auto-generated if not provided or if file doesn't exist)
* **--purchased-how-long-ago:** Number of days to look back for recently purchased books (defaults to 7).
* **--destination-book-directory:** The destination directory where you want to organize your audiobooks.
* **--download-program:** Specify the download program - OpenAudible or Libation (defaults to OpenAudible).
* **--audio-file-extension:** Audio file extension (defaults to .m4b).
* **--copy-instead-of-move:** Copy files instead of moving them (useful for debugging/testing). When profanity cleaning is enabled, this will also preserve chunk files and all intermediate processing files for inspection. Defaults to False.
* **--libation-folder-cleanup:** Whether to delete the source folder in Libation directory after processing (defaults to False).
* **--libation-file-locations-path:** Path to Libation's FileLocationsV2.json file (optional, for Libation users). When provided, the script will use the exact file paths from this file instead of constructing them. This is more reliable than the legacy path construction method.
* **--library-id:** The ID of your library in AudioBookShelf.
* **--log-file-path:** Path to the log file.
* **--server-url:** The base URL of your AudioBookShelf instance.
* **--source-audio-book-directory:** The directory where your download program saves audiobook files.
* **--generate-yaml:** This option writes your current flag choices to a YAML file _without_ running the program.
* **--yaml:** Path to YAML configuration file.
* **--debug:** Enable debug mode. When enabled with profanity cleaning, generates a detailed censorship report showing all profanity detected and censored (defaults to False).

### Profanity Cleaning Arguments (Optional)

* **--enable-profanity-cleaning:** Enable profanity cleaning using MonkeyPlug (defaults to False).
* **--remote-whisper-url:** Remote Whisper-WebUI service URL for transcription (required if cleaning enabled, e.g., http://whisper-server:8000).
* **--swears-file:** Path to custom swears list file (JSON or text format). Leave empty to use MonkeyPlug's default list.
* **--working-directory:** Working directory for profanity cleaning processing (defaults to /tmp/monkeyplug-cleaning).
* **--save-transcripts:** Save transcripts alongside cleaned audio files (defaults to False).
* **--timeout:** Timeout for transcription in seconds (defaults to 600 = 10 minutes).
* **--poll-interval:** How often to check remote Whisper server for transcription status in seconds (defaults to 30).
* **--beep-mode:** Use beep instead of mute for profanity (defaults to False).
* **--confidence-threshold:** Minimum confidence level (0.0-1.0) required to censor a word (defaults to 0.65).
* **--parallel-encoding:** Enable parallel chunk encoding to use multiple CPU cores (defaults to True).
* **--no-parallel-encoding:** Disable parallel chunk encoding (use single worker).

## Workflow

1. **Export Book Data:** Export your OpenAudible or Libation library as a JSON file. 
   * **OpenAudible:** Automatically creates `books.json` when you first login.
   * **Libation:** The script will automatically generate `libation.json` if it doesn't exist (requires `libationcli` in PATH). You can also manually export:
     ```bash
     libationcli export --path $HOME/libation/libation.json --json
     ```
 
2. **Configuration:** Set the required command-line arguments or use a YAML configuration file.
3. **(Optional) Set Up Whisper-WebUI:** If using profanity cleaning, ensure you have a Whisper-WebUI service running and accessible.
4. **Execution:** Run the script. It will:
   * (Libation only) Auto-generate `libation.json` if needed
   * Process books purchased within the specified timeframe
   * (Optional) Clean profanity from audio files using MonkeyPlug
   * Move audiobook files to the organized directory structure
   * Scan the AudioBookShelf library
   * Match the moved books in AudioBookShelf with Audible metadata
5. **Review Logs:** Check the log file for any errors or information about the processed books.

## Profanity Cleaning

This project integrates with [MonkeyPlug](https://github.com/stratus-ss/monkeyplug) to automatically remove profanity from audiobooks before adding them to AudioBookShelf. This feature uses remote transcription via [Whisper-WebUI](https://github.com/jhj0517/Whisper-WebUI) to identify and censor profane words.

### How It Works

1. **Transcription:** Audio files are sent to a remote Whisper-WebUI service for speech-to-text transcription.
2. **Detection:** Each word is checked against a configurable profanity list with confidence thresholds.
3. **Cleaning:** Profane words are either muted (silence) or replaced with beeps.
4. **Processing:** Cleaned files are moved to AudioBookShelf, with optional transcript preservation.
5. **Reporting (Debug Mode):** When `--debug` is enabled, generates a detailed censorship report showing all profanity detected and removed.

### Large File Handling

For audiobooks larger than 150MB, the system automatically uses intelligent chunking:

1. **Silence Detection:** Uses ffmpeg's silencedetect filter to find natural pauses in the audiobook
2. **Smart Splitting:** Splits audio at silence points near target chunk sizes (~130-150MB each)
3. **No Mid-Word Cuts:** Ensures splits happen during pauses between sentences, not in the middle of words
4. **Individual Processing:** Each chunk is transcribed and cleaned separately
5. **Seamless Reassembly:** Chunks are concatenated back into a single file
6. **Chunk Reuse:** If processing is interrupted, existing chunks and transcripts are reused on retry

This approach:
- Avoids upload timeout issues with remote transcription services
- Maintains professional audio quality (no word interruptions)
- Significantly improves reliability for long audiobooks
- Preserves all audio content (validated against original duration)

### Setup Requirements

1. **Whisper-WebUI Service:** Deploy a Whisper-WebUI instance (ideally with GPU for faster processing).
2. **Enable Cleaning:** Set `enable_profanity_cleaning: true` in your YAML config.
3. **Configure URL:** Provide the Whisper-WebUI URL (e.g., `http://whisper-server:8000`).
4. **(Optional) Custom Swear List:** Provide your own profanity list in JSON or text format.

### Performance Considerations

* Transcription is resource-intensive and time-consuming (typically 0.5-2x realtime speed even with GPU).
* A 10-hour audiobook may take 5-20 hours to process depending on hardware.
* Large files (>150MB) are automatically chunked to avoid timeouts while maintaining audio quality.
* Processing happens sequentially during the ingestion pipeline.
* Already-cleaned files are skipped automatically (MonkeyPlug embeds metadata tags).
* Interrupted processing can resume using existing chunks and transcripts.

### Example Configuration

```yaml
# Debug mode (enables censorship report)
debug: false  # Set to true to generate detailed censorship reports

# Enable profanity cleaning
enable_profanity_cleaning: true
remote_whisper_url: "http://192.168.1.100:8000"
working_directory: "/tmp/monkeyplug-cleaning"
save_transcripts: true  # Keep transcripts for reuse if processing fails
timeout: 600  # 10 minutes per chunk (increase for longer audiobooks)
poll_interval: 30  # Check transcription status every 30 seconds
confidence_threshold: 0.65  # Only censor words detected with ≥65% confidence
beep_mode: false  # Set to true to beep over profanity instead of muting
copy_instead_of_move: false  # Set to true to preserve chunks/transcripts for debugging
```

### Technical Details

**Architecture:**
- The audio cleaning is orchestrated by the `AudioCleaner` class in `modules/audio_cleaner.py`
- Actual chunking implementation is delegated to MonkeyPlug's `AudioChunker` class
- This separation ensures the codebase remains maintainable and avoids code duplication
- The 150MB threshold is defined in MonkeyPlug's `AudioChunker.needs_chunking()` method

**Silence Detection Parameters:**
- Threshold: -40dBFS (catches pauses between sentences)
- Minimum duration: 0.5 seconds
- Automatically finds 100s-1000s of split points per audiobook

**Stream Compatibility:**
- Automatically handles M4B files with multiple streams (audio, cover art, metadata)
- Excludes incompatible text/subtitle streams during processing
- Preserves cover art and essential metadata

**Validation:**
- Verifies output file size is within acceptable range (>50% of original)
- Falls back to original file if cleaning produces suspicious results
- Logs detailed file size comparisons for troubleshooting

## File Organization

The script organizes audiobooks in the following structure:
```
destination_dir/
├── Author_Name/
│   ├── Series_Name/
│   │   └── Book_Title/
│   │       └── audiobook_file.m4b
│   └── Standalone_Book/
│       └── audiobook_file.m4b
└── Another_Author/
    └── ...
```

## JSON File Structure Examples

### OpenAudible JSON Format:
```json
{
  "asin": "B0123456789",
  "author": "Author Name",
  "summary": "Book summary...",
  "filename": "book_filename",
  "purchase_date": "2024-01-01",
  "series_name": "Series Name",
  "title_short": "Short Title",
  "title": "Full Book Title",
  "series_sequence": 1
}
```

### Libation JSON Format:
```json
{
  "AudibleProductId": "B0123456789",
  "AuthorNames": "Author Name",
  "Description": "Book description...",
  "Title": "Book Title",
  "Subtitle": "Book Subtitle",
  "DateAdded": "2024-01-01T12:00:00.0+00:00",
  "SeriesNames": "Series Name",
  "SeriesOrder": "1"
}
```

### Libation FileLocationsV2.json Format:
Libation maintains a FileLocationsV2.json file that contains the exact file paths for all downloaded audiobooks. Using this file is recommended as it eliminates the need for path construction and handles edge cases (like titles with colons) more reliably.

```json
{
  "Dictionary": {
    "B0123456789": [
      {
        "Id": "B0123456789",
        "FileType": 2,
        "Path": {
          "Path": "/path/to/DownloadsInProgress/B0123456789.aaxc"
        }
      },
      {
        "Id": "B0123456789",
        "FileType": 1,
        "Path": {
          "Path": "/path/to/Books/Book Title [B0123456789]/Book Title: Subtitle [B0123456789].m4b"
        }
      }
    ]
  }
}
```

> [!NOTE]
> FileType 1 indicates the actual audiobook file, while FileType 2 indicates temporary download files. The script automatically extracts the correct path for the audiobook file.

## Testing

The project includes comprehensive test coverage focusing on integration with external libraries rather than duplicating their internal tests.

**Test Files:**
- `tests/test_audio_cleaner_integration.py` - Tests AudioCleaner orchestration and MonkeyPlug integration
- `tests/test_monkeyplug_integration.py` - End-to-end integration tests with MonkeyPlug
- `tests/test_config.py` - Tests configuration validation and parsing
- `tests/test_integration_real_data.py` - Integration tests with real audiobook data
- `tests/test_utils.py` - Tests utility functions (path sanitization, directory creation)
- `tests/test_audio_bookshelf.py` - Tests AudioBookShelf API integration
- `tests/test_move_audio_book_files.py` - Tests file organization and moving logic

**Running Tests:**
```bash
# Install development dependencies
pip install -r requirements-dev.txt

# Run all tests
pytest

# Run with coverage report
pytest --cov=modules --cov-report=html

# Run specific test file
pytest tests/test_audio_cleaner_integration.py -v

# Run integration tests only
pytest tests/test_monkeyplug_integration.py -v
```

**Test Philosophy:**

This project follows the principle of **testing the integration layer, not external dependencies**:

- ✅ **We test:** Configuration passing, error handling, path setup, statistics tracking, logging
- ❌ **We don't test:** MonkeyPlug's chunking algorithms, AudioChunker internals, ffmpeg operations

**Key Test Areas:**
- **AudioCleaner Integration:** Proper initialization, path setup, MonkeyPlug configuration
- **Chunking Detection:** Verifies AudioChunker is called for large files (>150MB)
- **Error Handling:** Fallback to original file when cleaning fails
- **Configuration Validation:** Required parameters for profanity cleaning
- **Statistics Tracking:** Counts for processed, failed, and cleaned files
- **File Management:** Copy vs move modes, cleanup behavior, transcript preservation
- **Path Sanitization:** Unix-safe filenames, directory structure creation

## Contributing

Contributions are welcome! Feel free to open issues or submit pull requests.

**Development Guidelines:**
- Follow the existing code style (black, flake8, mypy)
- Keep function complexity under 15 (as measured by cyclomatic complexity)
- Functions should not exceed 50 lines unless generating text output
- All imports should be at the top of the file
- Add tests for new functionality
- Update documentation for configuration changes

## AI Disclosure

This project utilizes AI assistance in the following ways:

- **Test Generation:** AI tools were used to help generate comprehensive test suites, including integration tests and edge case coverage
- **Code Review:** AI-assisted analysis for code maintainability, complexity assessment, and architectural recommendations
- **Research & Documentation:** AI was used to research best practices, generate documentation examples, and clarify technical concepts

All AI-generated content has been reviewed, validated, and adapted by human developers to ensure correctness, maintainability, and alignment with project goals. The use of AI tools does not replace human judgment but serves as an assistive technology in the development process.

## License
AGPL V3